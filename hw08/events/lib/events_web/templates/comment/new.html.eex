<h1>New Comment</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.comment_path(@conn, :create)) %>

<br/>
<span><%= link "Back", to: Routes.event_path(@conn, :show, @event_id), class: "btn btn-secondary" %></span>
