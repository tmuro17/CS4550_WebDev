<h1>Create Invite</h1>
<%= render "create_form.html", Map.put(assigns, :action, Routes.invite_path(@conn, :create)) %>

<br/>
<span><%= link "Cancel", to: Routes.invite_path(@conn, :index), class: "btn btn-secondary" %></span>
